Include Once

C3D_MAX_CAMERAS = 5

Dim C3D_Camera_Position[C3D_MAX_CAMERAS, 3]
Dim C3D_Camera_Rotation[C3D_MAX_CAMERAS, 3]
C3D_Camera_Count = 0



Sub C3D_SetCameraPosition(camera_num, x, y, z)

End Sub

Sub C3D_SetCameraRotation(camera_num, x, y, z)

End Sub